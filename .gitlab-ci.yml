pages:
  stage: deploy
  script:
    - mkdir -p public
    - cp index.html public/
    - cp -r data public/
    - cp README.md public/ || true
  artifacts:
    paths:
      - public
  only:
    - main
